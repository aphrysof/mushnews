export type userDetailProps = {
    name: string;
    username: string;
    email: string;
    password: string;
    isChecked: boolean;

}
export type userAuthentication = {
    email: string;
    password: string;
}
