export type userDetailProps = {
    name: string;
    username: string;
    email: string;
    password: string;
    isChecked: boolean;
    latitude: number;
    longitude: number;
}
export type userAuthentication = {
    email: string;
    password: string;
}
